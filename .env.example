# Optional: only needed by adapters that execute Apify actors.
# Never commit a real token.
# Resolution order: current process -> Windows User -> ignored .env.local.
# Preflight must call GET https://api.apify.com/v2/users/me and require HTTP 200.
APIFY_TOKEN=
