# 项目路线图

## 一、任务定义

输入一个采购需求，Agent自动理解采购参数，制定多平台搜索策略，从公开采购或电商渠道搜集候选商品和供应商，完成商品参数匹配、重复商品清洗、价格标准化、供应商初筛和综合评分，最终输出带原始来源、采集时间、可信度和推荐理由的采购比价表。无法确认的信息必须明确标记，并生成待人工询价或确认清单。

第一阶段止于“形成候选供应商池和初步比价结论”，不自动决定供应商或下单。

## 二、核心能力

1. 商品匹配：判断候选商品是否真正满足采购需求。
2. 数据归一化：统一不同平台的商品、供应商、数量、币种、税费和交付口径。
3. 采购判断：结合匹配度、TCO、供应商可靠度、交期和风险提供决策支持。

## 三、阶段规划

### MVP

1. Procurement Requirement Schema
2. 最终比价表与证据结构
3. 候选商品与供应商统一Schema
4. Query Planner
5. 商品匹配与去重
6. TCO成本及综合评分模型
7. 平台Adapter
8. Skill组装和真实案例验证

## 四、当前执行关卡

必须按以下顺序推进，不因单个案例达到 `SEARCH_READY` 而跳过中间阶段：

| 阶段 | 工作 | 退出条件 | 当前状态 |
|---:|---|---|---|
| 0 | 建立GitHub项目基线 | 目录、README、规划、Schema和历史可追溯 | 已完成 |
| 1 | 用真实采购案例验证需求Schema | 案例、缺失字段清单、补问结果真实落盘 | 已完成 |
| 2 | 修订并冻结Requirement Schema v0.2 | 四例回归、标准验证器校验、人工字段审阅 | 已完成 |
| 3 | 定义最终采购输出结构 | 采购结论、核心比价表、风险提示和证据清单可供初筛与审批 | v0.1候选版已形成，待业务审阅 |
| 4 | 定义并冻结Product Candidate Schema v0.1 | 四类结构回归、四类真实回归、正式Schema与业务校验、人工审阅 | 已完成 |
| 5 | 定义并冻结Supplier Schema v0.1 | 支持主体、资质、评价、销量、能力和风险信息 | 已完成 |
| 6 | 设计并冻结Query Planner v0.1 | 一条需求可生成多平台 × 关键词 × 筛选条件，并记录执行结果与降级 | 已完成 |
| 7 | 商品匹配与去重 | 候选与需求逐项匹配，跨平台重复候选可识别并保留来源 | 已完成 |
| 8 | TCO成本与综合评分模型 | 统一价格、运费、税费和未知成本，输出单一综合分及可解释底稿 | 已完成 |
| 9 | 平台Adapter | 将API、网页、浏览器和截图数据统一映射为Candidate、Supplier及证据记录 | 已完成 |
| 10 | Skill组装与端到端验证 | 串联需求、查询、采集、匹配、去重、TCO和最终采购输出 | 已完成 |

MVP暂不实现自动聊天询价、自动联系供应商、砍价、下单、审批或ERP写入。

### V1：正式询价闭环

网上初步比价 → 筛选供应商 → 生成统一RFQ → 人工发送或授权发送 → 解析正式报价 → 二次比价 → 推荐供应商。

### V2：采购Copilot

接入历史采购价、企业供应商库、RFQ、风险检查、采购审批与异常报价识别。

## 五、阶段4冻结记录

Product Candidate Schema v0.1于2026-08-26完成冻结。标准品、设备、定制品和服务类均完成实例验证；咖啡机、蓝牙音响、IP人偶服和住宅设计服务完成真实或轻量真实回归。Draft 2020-12正式验证与跨记录业务校验均通过。

## 六、阶段5冻结记录

Supplier Schema v0.1于2026-08-26完成冻结。供应商主体与平台店铺分离建模，标准品、设备、定制品和服务类共8份记录完成正式Schema与业务规则校验。主动询盘仍不属于本阶段范围。

## 七、阶段6冻结记录

Query Planner v0.1于2026-08-26完成冻结。四类采购需求均可生成平台、关键词、筛选条件、访问策略和降级规则；咖啡机案例完成既有真实搜索结果回放。5份记录通过正式Schema与业务规则校验。

## 八、阶段7冻结记录

商品匹配与去重v0.1于2026-08-26完成冻结。四类匹配记录、精确重复与疑似同款结构示例，以及3个咖啡机真实去重簇共9份记录通过正式Schema和业务规则校验。去重仅合并比较视图，不删除原始来源。

## 九、阶段8冻结记录

TCO成本与综合评分模型v0.1于2026-08-26完成冻结。人民币统一比较价、括号内平台原价、大致参考汇率、未知成本和五维单一总分规则均已落地。四类结构记录与3份咖啡机真实组合共7份记录通过正式Schema及业务规则校验。

## 十、阶段9冻结记录

平台Adapter v0.1于2026-08-26完成冻结。淘宝与1688 API、公开网页、登录浏览器和访问失败路径均完成回归；字段级映射、输出引用、脱敏和真实状态表达通过校验。Adapter不包含任何Token或登录凭据。

## 十一、阶段10冻结记录

采购询源与比价 Skill v0.1于2026-08-26完成冻结。统一入口已串联前九阶段合同；咖啡饮料机历史案例建立八阶段端到端清单，并通过Skill格式、清单引用和全部底层校验器。历史候选ID不完全一致、库存、精确运费、最终交期及供应商确认仍未闭环，因此该历史回归状态如实标记为 `PARTIAL`。

2026-08-27使用“2件、客户礼品、中国大陆7天内到货、单价暂按¥1,000以内”的智能手环新需求完成盲测。盲测后追加三项兼容性规则：多件预算口径显式化、原始结果与有效候选分离、平台选择考虑数量/MOQ/交期。冻结Schema语义未改变。

