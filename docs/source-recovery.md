# 历史内容恢复说明

## 来源

本仓库初始内容依据 Codex/ChatGPT 项目中的两个历史任务恢复：

- “采购比价任务规划”
- “规划采购比价Skill”

规划正文能够从任务消息中读取，因此路线图和工作流按原讨论内容重新组织，未改变核心任务边界。

## 未能取得的原始附件

历史任务提到四个 `Procurement Requirement Schema v0.1` 云端产物，并显示内容引用标识，但当前任务读取接口没有返回附件实体、文件名或原始字节。因此：

- 无法证明本仓库中的 Schema 与历史附件逐字节相同；
- `schemas/` 和 `examples/` 是依据历史对话中已确认的设计摘要重新构建；
- 历史记录中的版本标识 `e69bd80` 无法在当时未同步的空 GitHub 仓库中核验；
- 若未来取得原附件，应做结构化差异比较，不应直接覆盖当前文件。

## 可确认的历史设计约束

- 固定采购骨架，并用属性约束兼容多种品类；
- 区分 `HARD`、`PREFERENCE`、`INFORMATION`；
- 五种需求状态；
- 每轮最多三个阻断问题；
- 独立 provenance 记录字段来源和可信度；
- 不得静默假设数量、交期、税率、替代品牌或定制工艺。
